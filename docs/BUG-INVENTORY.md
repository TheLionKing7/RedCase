# RedCase Bug Inventory — 2026-09-24

Scope: evidence-led consistency audit, recorded before fixes. No application/audit finding in this inventory has been fixed. Screenshots are under `docs/audit-evidence/`, captured from the local development build (`VITE_API_DEV_ADAPTER=1`). Theme/navigation comparisons use `docs/RedCase-Shell-UX-Spec.md` §4 and `docs/RedCase-IA-and-Roles-Spec.md` §2. Severity is initial triage.

| id | severity | surface | evidence (screenshot path or log line) | suspected cause | suggested fix |
|---|---|---|---|---|---|
| RC-001 | high | Settings route / navigation | `docs/audit-evidence/settings.png`, `settings-profile.png`, `settings-appearance.png` show 404; `apps/web/src/routeTree.gen.ts` lists no settings route. `AppShell.tsx` rail omits Settings despite 7-item IA model. | Personal Settings/profile/theme route not implemented; shell route tree/nav drifted from IA spec. | Implement Settings root with Profile, Agent Persona, Notifications, Integrations and Appearance panels; add Settings to rail and route tree. |
| RC-002 | high | Luna provider deployment (VPS) | `docs/calibration/phase1-jina.md` §11: SSH to `api.redcase.xyz:22` timed out; no `/opt/redcase/.env` or live provider proof available. | Production change/container recreation could not be executed or verified from this session. | With VPS access, set primary `explabs`, fallback `groq`, remove dead provider key entries, recreate container, query `/v1/query`, verify `serving_provider=explabs`, then collect production latency. |
| RC-003 | high | Provider data processing / Vault A privacy | Calibration §11: explabs free-tier training/retention terms remain owner-unverified. | Provider data-processing configuration has not been confirmed against private-content policy. | Verify console terms. If inputs train, restrict provider to Vault B/public research with an explicit tested server-side config gate; do not send Vault A content. |
| RC-004 | high | Web static/dead-route consistency | Screenshots `settings.png`, `settings-profile.png`, `settings-appearance.png` are 404; no `_authed.settings` route exists in `apps/web/src/routes/`. | Requested destinations are not implemented, though UX spec names Appearance under Settings. | Implement route(s), or intentionally redirect/remove links after deciding settings IA; regenerate route tree. |
| RC-006 | high | Authenticated shell theme | `docs/audit-evidence/home.png`, `contacts.png`, `search.png`, `workbench.png`, `tracker.png`, `red-teamer.png`, `firm-command.png`, `channels.png`, `threads.png`, `pins.png`, `access-requests.png` show dark canvas. UX spec §4 requires dark chrome and light default content canvas; dark canvas is an Appearance option. | Dark surface tokens apply globally; no effective default light reading canvas. | Scope tokens so chrome remains dark and canvas defaults light; retain explicit dark/system preference and validate contrast. |
| RC-007 | medium | AppShell top-level IA | `AppShell.tsx` lines 11–28 show six rail entries; IA §2 lists seven including Settings. | Shell route model omits design-authority Settings item. | Add Settings for all roles; continue to gate Firm Ops separately. |
| RC-011 | medium | Contact directory API path (not a current 404) | `apps/web/src/components/CollaborationWorkspace.tsx` calls `GET /v1/members`; live OpenAPI confirms it exists, as well as `/v1/members/me`. `contacts.png` captured while loading. | Earlier reported Contacts mismatch is absent from current API contract; could be stale build or response failure. | Keep OpenAPI-backed contract test and inspect authenticated response before changing path. |
| RC-012 | medium | Build/type safety | `apps/web/node_modules/.bin/tsc --noEmit` reports four errors: `AppShell.tsx:24` invalid `"/access-requests"` ShellRoute; `AnalysisSectionCard.tsx:65` optional label includes undefined; `CommandPalette.tsx:30` unsupported `title`/`description`; `identity.ts:28` possibly undefined `w[0]`. | Recent shell/palette/types changes have not passed strict TS checks. | Fix the four independent type/API mismatches in a dedicated batch; add CI type-check. |
| RC-013 | medium | Alert/help controls | AppShell header screenshot has Notifications button with no action handler (`apps/web/src/components/AppShell.tsx`); no notification surface. | Visible chrome control is placeholder-only. | Implement notification panel/state or clearly disabled/coming-soon affordance with accessible behavior. |
| RC-014 | medium | Tracker readiness | `docs/audit-evidence/tracker.png`; IA §1.8 gates diary/deadline use on counsel validation. | Tracker may appear operational before jurisdiction rules are validated. | Verify warning/status against live responses; keep derived dates marked pending counsel validation until approved. |
| RC-015 | low | Local audit log encoding | `web_audit_server.log` Vite ready arrow renders as `âžœ`; targeted API source sweep found 62 files / 0 suspicious sequences. | Windows terminal code page/output encoding artifact, not confirmed source corruption. | Keep UTF-8 terminal/log encoding; do not mass-rewrite source based on console output. |
| RC-016 | low | Calibration baseline comparison | Luna calibration §11 compares DeepSeek at 41/50; task handoff flagged notes containing 41/50 vs 45/50. | Runs or scoring versions may have been conflated; no raw-evidence reconciliation done. | Reconcile raw JSON with model, prompt, run date, scorer, and denominator before comparison claims. |
| RC-017 | low | Production dev-adapter assurance | Audit screenshots explicitly use local `VITE_API_DEV_ADAPTER=1`; source has dev adapters/demo identity. Deployed production JS/config was not inspected. | Build-time VITE selection determines adapter behavior; production config not independently verified. | Add production-build assertion that dev adapter is off; inspect deployed bundle for selected fixtures/demo behavior. |
| RC-018 | medium | Onboarding Storage live verification | Onboarding/API tests verify upload integration behavior, signature sniffing and firm-admin authorization in unit tests; no live Supabase Storage object/bytes or non-admin denial evidence is recorded. | Storage bucket provisioning, byte persistence and live authorization have not been verified against Supabase. | Keep the onboarding task open until a live upload proves expected bytes in the private bucket and a non-admin receives denial; record evidence before marking this slice complete. |

## Audit checks and scope

- API: `.venv312/Scripts/python.exe -m pytest -q` — **276 passed, 51 skipped, 9 warnings** (`apps/api/pytest_audit_run.txt`).
- Frontend Vite/Nitro build completed and generated output; known Radix `use client` warnings appeared. Command host ended before clean exit status capture.
- TypeScript **failed**, four errors listed as RC-012.
- Alembic: one head, **`0027 (head)`**.
- Mojibake: targeted API app source sweep **62 files, 0 hits**; broader `apps/` scan was attempted but shell completion could not be captured. Console artifact listed in RC-015.
- Live OpenAPI fetch succeeded, 57 paths. Confirms `/v1/members` GET and `/v1/members/me` GET; confirms `/v1/firm/matters/progress`, not `/v1/matters/progress`.
- 16 screenshots: public `/`, `/signin`, `/onboarding`; authenticated `/home`, `/workbench`, `/search`, `/red-teamer`, `/tracker`, `/firm-command`, `/channels`, `/threads`, `/contacts`, `/pins`, `/access-requests`; plus `/settings`, `/settings/profile`, `/settings/appearance` 404s. Local dev adapter enabled; not production-visual verification.
- Dead-link inspection used TanStack `src/routes` and `routeTree.gen.ts`; Settings missing. Exhaustive crawl of every dynamic request/method and clickable href remains outstanding; no claim of complete method-level verification.
- RC-001, RC-004 and RC-007 are addressed in this slice by adding Settings route and shell navigation; other findings remain open.
- Onboarding Storage integration is unit-verified only; live bucket bytes and non-admin denial remain pending (RC-018). Keep the task open until live evidence is recorded.

| RC-008 | medium | Messenger sub-panel | `AppShell.tsx` line 25 defines Matter channels/Direct/Archived without destinations; task-bar routes exist. | Panel items have neither `to` route nor action. | Wire supported destinations; render unavailable archive explicitly disabled/BETA rather than dead affordance. |
| RC-009 | medium | Cockpit shell geometry and Home | `docs/audit-evidence/home.png` shows dense dark workspace and broad adjacent panels; UX spec sketch requires obsidian rail/header, distinct off-black sub-panel, light document canvas. | Layout does not fully express the specified three-layer chrome and calm reading canvas. | Reconcile layout against UX spec; preserve progressive disclosure while clarifying panel/canvas boundaries. |
| RC-010 | medium | Home/directory loading states | `home.png` and `contacts.png` show “Loading recent work…”, “Loading your schedule…”, “Loading firm directory…” at capture time. | Local adapter/query completion may be slow or unresolved; capture may show transient state. | Verify after network idle with production-like responses; implement skeleton/empty/error states with retry and avoid indefinite loading labels. |
| RC-011 | medium | Contact directory API path (not a current 404) | `apps/web/src/components/CollaborationWorkspace.tsx` calls `GET /v1/members`; live OpenAPI confirms it exists, as well as `/v1/members/me`. `contacts.png` captured while loading. | Earlier reported Contacts mismatch is absent from current API contract; could be stale build or response failure. | Keep OpenAPI-backed contract test and inspect authenticated response before changing path. |
| RC-012 | medium | Build/type safety | `apps/web/node_modules/.bin/tsc --noEmit` reports four errors: `AppShell.tsx:24` invalid `"/access-requests"` ShellRoute; `AnalysisSectionCard.tsx:65` optional label includes undefined; `CommandPalette.tsx:30` unsupported `title`/`description`; `identity.ts:28` possibly undefined `w[0]`. | Recent shell/palette/types changes have not passed strict TS checks. | Fix the four independent type/API mismatches in a dedicated batch; add CI type-check. |
| RC-013 | medium | Alert/help controls | AppShell header screenshot has Notifications button with no action handler (`apps/web/src/components/AppShell.tsx`); no notification surface. | Visible chrome control is placeholder-only. | Implement notification panel/state or clearly disabled/coming-soon affordance with accessible behavior. |
| RC-014 | medium | Tracker readiness | `docs/audit-evidence/tracker.png`; IA §1.8 gates diary/deadline use on counsel validation. | Tracker may appear operational before jurisdiction rules are validated. | Verify warning/status against live responses; keep derived dates marked pending counsel validation until approved. |
| RC-015 | low | Local audit log encoding | `web_audit_server.log` Vite ready arrow renders as `âžœ`; targeted API source sweep found 62 files / 0 suspicious sequences. | Windows terminal code page/output encoding artifact, not confirmed source corruption. | Keep UTF-8 terminal/log encoding; do not mass-rewrite source based on console output. |
| RC-016 | low | Calibration baseline comparison | Luna calibration §11 compares DeepSeek at 41/50; task handoff flagged notes containing 41/50 vs 45/50. | Runs or scoring versions may have been conflated; no raw-evidence reconciliation done. | Reconcile raw JSON with model, prompt, run date, scorer, and denominator before comparison claims. |
| RC-017 | low | Production dev-adapter assurance | Audit screenshots explicitly use local `VITE_API_DEV_ADAPTER=1`; source has dev adapters/demo identity. Deployed production JS/config was not inspected. | Build-time VITE selection determines adapter behavior; production config not independently verified. | Add production-build assertion that dev adapter is off; inspect deployed bundle for selected fixtures/demo behavior. |

## Audit checks and scope

- API: `.venv312/Scripts/python.exe -m pytest -q` — **276 passed, 51 skipped, 9 warnings** (`apps/api/pytest_audit_run.txt`).
- Frontend Vite/Nitro build generated output; known Radix `use client` warnings appeared. Command host ended before clean exit status capture.
- TypeScript **failed**, four errors listed as RC-012.
- Alembic: one head, **`0027 (head)`**.
- Mojibake: targeted API app source sweep **62 files, 0 hits**; broader `apps/` scan attempted but shell completion could not be captured. Console artifact listed in RC-015.
- Live OpenAPI fetch succeeded, 57 paths. Confirms `/v1/members` GET and `/v1/members/me` GET; confirms `/v1/firm/matters/progress`, not `/v1/matters/progress`.
- Live OpenAPI fetch succeeded, 57 paths. Confirms `/v1/members` GET and `/v1/members/me` GET. Confirmed key endpoints were compared against the live schema; exhaustive all-client/all-method crawl is outstanding.
- 16 screenshots: public `/`, `/signin`, `/onboarding`; authenticated `/home`, `/workbench`, `/search`, `/red-teamer`, `/tracker`, `/firm-command`, `/channels`, `/threads`, `/contacts`, `/pins`, `/access-requests`; plus `/settings`, `/settings/profile`, `/settings/appearance` 404s. Local dev adapter enabled; not production-visual verification.
- Dead-link inspection used TanStack `src/routes` and `routeTree.gen.ts`; Settings missing. Exhaustive crawl of dynamic requests/methods and clickable hrefs remains outstanding; no claim of complete method-level verification.
- No inventory finding has been fixed in this change set.

## W-gap follow-up — 2026-09-25

- Workbench / Deck W-gap updates are implemented in the local slice; migration `0033` adds `DRAFT` to `document_analyses.status` and is applied to the configured Supabase database (`0033` verified as head). DOCX extraction and upload-byte deduplication are verified by the Vault A API tests (13 passed); this intake behavior is covered at the appropriate API level, not by a screenshot.
- **W-gap browser evidence: re-capture deck + display frame with seeded analysis.** Existing Deck capture showed loading and the current logged-in fixture did not provide a populated analysis or an exercised Send-to-Inbox result, so those browser interactions remain unverified.

