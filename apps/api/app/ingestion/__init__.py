"""Vault B ingestion package — deterministic-first chunking and metadata.

Task 1.3 (Phase1-Design 2.2 chunking strategy + 4 ingestion spec).
Deterministic-first, LLM-second, never the reverse (design 4).
"""
